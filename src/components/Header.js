const Header = () => {
  return (
    <header className="header">
      <h2>/DevBlog/</h2>
      <nav className="nav">
        <a href="#" className="links">Home</a>
        <a href="#" className="links">New Post</a>
      </nav>
    </header>
  );
};

export default Header;
